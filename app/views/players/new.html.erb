<h1>New Player</h1>
<%= form_with model: @player, local: true do |f| %>
  <div>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div>
    <%= f.submit t('players.new.create') %>
  </div>
<% end %>

<%= link_to t('players.new.back'), players_path %>