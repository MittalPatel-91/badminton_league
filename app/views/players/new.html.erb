<h1>New Player</h1>

<%= form_with model: @player, local: true do |f| %>
  <div>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div>
    <%= f.submit 'Create' %>
  </div>
<% end %>

<%= link_to 'Back', players_path %>
